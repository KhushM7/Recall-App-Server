# Create a class which will have all the properties of a flashcard
# Another class which will inherit those properties
# Functions which will allow the user to edit the inherited object and then save it into the flashcard database
